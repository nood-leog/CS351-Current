/* guitars js*/

"use strict";

$(document).ready(function(){
  $('.bxslider').bxSlider();
});

/*
$(document).ready(function ()
{
    $('.bxslider').bxSlider(
            {
                pager: true, // Display navigation dots

                slideWidth: 400, // Adjust slide width if needed

                auto: true, // Enable auto-scrolling

                autoControls: true, // Show start/stop controls

                autoStart: true, // Start auto-scrolling immediately

                pause: 3000, // Auto-scroll interval in milliseconds

                mode: 'horizontal' // Default horizontal scrolling
                        
            });
});
 * 
 */